version: 2.1

orbs:
  ruby: circleci/ruby@1.8.0

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      # Add step below to install Ruby version 1.8.0
      - ruby/install:
          version: '1.8.0'

      
      - run: ruby -v

workflows:
  run-jobs:
    jobs:
      - build
